<project
	xmlns="http://maven.apache.org/POM/4.0.0"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">
	<modelVersion>4.0.0</modelVersion>
	<parent>
		<groupId>org.safris.xml</groupId>
		<artifactId>toolkit</artifactId>
		<version>${xmlVersion}</version>
	</parent>
	<groupId>org.safris.xml.toolkit</groupId>
	<artifactId>binding</artifactId>
	<version>${xmlVersion}</version>
	<name>${groupId}.${artifactId}</name>
	<description>
		Binding is an application wrapper for the technology for accessing XML by binding the XSD of the XML to
		Java classes. But what makes XML Binding different from any other binding technology available
		elsewhere, like XMLBeans, Castor, and JAX-B? Unlike the other technologies, XML Binding approaches
		the xml binding problem with 3 simple rules: to work on any xml schema as per the XSD standard,
		to create all bindings as strongly typed classes or enums (i.e. bring all errors to compile time), and
		to be intuitive to use by developers. The organization of this project defines two modules, "Runtime,"
		and "Generator." Please refer to those modules for more details about their roles in making XML Binding
		a successful framework.
	</description>
	<build>
		<plugins>
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-dependency-plugin</artifactId>
				<executions>
					<execution>
						<phase>package</phase>
						<goals>
							<goal>unpack-dependencies</goal>
						</goals>
						<configuration>
							<includeGroupIds>org.safris</includeGroupIds>
							<outputDirectory>${project.build.directory}/classes</outputDirectory>
							<overWriteSnapshots>true</overWriteSnapshots>
							<overWriteReleases>true</overWriteReleases>
							<overWriteIfNewer>true</overWriteIfNewer>
						</configuration>
					</execution>
				</executions>
			</plugin>
			<plugin>
				<artifactId>maven-antrun-plugin</artifactId>
				<executions>
					<execution>
						<id>install</id>
						<configuration>
							<tasks>
								<delete dir="${project.build.directory}/classes/META-INF"/>
								<mkdir dir="${project.build.directory}/classes/META-INF"/>
								<manifest file="${project.build.directory}/classes/META-INF/MANIFEST.MF">
									<attribute name="Main-Class" value="org.safris.xml.toolkit.binding.Generator"/>
								</manifest>
								<mkdir dir="${project.build.directory}/lib"/>
								<jar destfile="${project.build.directory}/lib/xml-${parent.artifactId}-${artifactId}-${version}.jar" basedir="${project.build.directory}/classes" index="true" filesetmanifest="merge"/>
							</tasks>
						</configuration>
					</execution>
					<execution>
						<id>deploy</id>
						<configuration>
							<tasks>
								<scp file="${project.build.directory}/lib/xml-${parent.artifactId}-${artifactId}-${version}.jar" todir="${env.USER}@safris.org:/var/lib/apache2/org/safris/www/xml/standalone/binding" trust="true" keyfile="${env.HOME}/.ssh/id_dsa" passphrase=""/>
							</tasks>
						</configuration>
					</execution>
				</executions>
			</plugin>
		</plugins>
	</build>
	<dependencies>
		<dependency>
			<groupId>org.safris.xml.generator</groupId>
			<artifactId>compiler</artifactId>
			<version>${xmlVersion}</version>
		</dependency>
	</dependencies>
</project>
