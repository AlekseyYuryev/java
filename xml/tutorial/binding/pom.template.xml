<project xmlns="http://maven.apache.org/POM/4.0.0"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">
	<modelVersion>4.0.0</modelVersion>
	<parent>
		<groupId>org.safris.xml</groupId>
		<artifactId>tutorial</artifactId>
		<version>${xmlVersion}</version>
	</parent>
	<groupId>org.safris.xml.tutorial</groupId>
	<artifactId>binding</artifactId>
	<version>${xmlVersion}</version>
	<name>${groupId}.${artifactId}</name>
	<description>TODO</description>
	<build>
		<outputDirectory>${basedir}/target/jar</outputDirectory>
		<resources>
			<resource>
				<directory>${basedir}</directory>
				<includes>
					<include>**/*.java</include>
					<include>**/*.xml</include>
					<include>**/*.xsd</include>
					<include>**/bin/*</include>
				</includes>
			</resource>
		</resources>
		<plugins>
			<plugin>
				<groupId>org.safris.xml.maven</groupId>
				<artifactId>binding</artifactId>
				<version>${xmlVersion}</version>
				<executions>
					<execution>
						<phase>generate-sources</phase>
						<goals>
							<goal>generate</goal>
						</goals>
					</execution>
				</executions>
				<configuration>
					<manifest xmlns="http://xml.safris.org/toolkit/application/binding/manifest.xsd">
						<link xmlns:xlink="http://www.w3.org/1999/xlink" xlink:href="${basedir}/src/main/resources/xml/manifest.xml"/>
					</manifest>
				</configuration>
			</plugin>
<!--		<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-jar-plugin</artifactId>
				<configuration>
					<classesDirectory>${project.build.directory}/install/classes</classesDirectory>
					<finalName>tutorial-${artifactId}-${version}</finalName>
					<outputDirectory>${project.build.directory}/install/lib</outputDirectory>
				</configuration>
			</plugin>-->
		</plugins>
	</build>
	<dependencies>
		<dependency>
			<groupId>org.safris.commons</groupId>
			<artifactId>util</artifactId>
			<version>${commonsVersion}</version>
		</dependency>
		<dependency>
			<groupId>org.safris.xml.generator</groupId>
			<artifactId>compiler</artifactId>
			<version>${xmlVersion}</version>
		</dependency>
	</dependencies>
</project>