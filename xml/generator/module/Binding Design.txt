Key:
     XSD classes  instance
Java  {classes}    [obj]
XML     (xsd)       <xml>


Step 1|6 [COMPILE]: This is a circular reference because this step depends on the last step of this process to be completed

                 {generator} w/ [Binding]
(XMLSchema.xsd) --------------------------> {XMLSchema}


Step 2 [IMPLEMENTATION & COMPILE]: Inside {generator} we have code that creates a xml instance (binding.xsd) that defines the binding Java-to-XML link

             marshal w/ {XMLSchema}
{generator} -----------------------> (binding.xsd)

Step 3 [IMPLEMENTATION]: Perform validation check of binding.xml implementation XML instances using XML validator & binding.xsd

               validation check w/ [binding.xml]
(binding.xsd) -----------------------------------> <binding.xml>

Step 4 [COMPILE]: Use generator to generate classes from binding.xsd

               {generator}
(binding.xsd) -------------> {Binding}

Step 5 [RUNTIME]: Parse the binding.xml inmplementation XML instances into runtime java Objects

               parse w/ {Binding}
<binding.xml> ------------------------> [Binding]

Step 1|6 [COMPILE]: This is a circular reference because this step depends on the last step of this process to be completed

                 {generator} w/ [Binding]
(XMLSchema.xsd) --------------------------> {XMLSchema}
