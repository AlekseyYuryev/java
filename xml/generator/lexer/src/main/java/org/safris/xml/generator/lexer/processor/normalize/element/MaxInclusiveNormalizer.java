package org.safris.xml.generator.lexer.processor.normalize.element;

import org.safris.xml.generator.lexer.processor.model.element.MaxInclusiveModel;
import org.safris.xml.generator.lexer.processor.normalize.Normalizer;
import org.safris.xml.generator.lexer.processor.normalize.NormalizerDirectory;

public class MaxInclusiveNormalizer extends Normalizer<MaxInclusiveModel>
{
	public MaxInclusiveNormalizer(NormalizerDirectory directory)
	{
		super(directory);
	}

	protected void stage1(MaxInclusiveModel model)
	{
	}

	protected void stage2(MaxInclusiveModel model)
	{
	}

	protected void stage3(MaxInclusiveModel model)
	{
	}

	protected void stage4(MaxInclusiveModel model)
	{
	}

	protected void stage5(MaxInclusiveModel model)
	{
	}

	protected void stage6(MaxInclusiveModel model)
	{
	}
}
