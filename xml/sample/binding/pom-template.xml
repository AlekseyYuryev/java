<project xmlns="http://maven.apache.org/POM/4.0.0"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">
	<modelVersion>4.0.0</modelVersion>
	<parent>
		<groupId>org.safris.xml</groupId>
		<artifactId>sample</artifactId>
		<version>${xmlVersion}</version>
	</parent>
	<groupId>org.safris.xml.sample</groupId>
	<artifactId>binding</artifactId>
	<version>${xmlVersion}</version>
	<name>${groupId}.${artifactId}</name>
	<description>This module contains sample code of the XML Binding application that may be used by developers as a tutorial.</description>
	<build>
		<plugins>
			<plugin>
				<artifactId>maven-antrun-plugin</artifactId>
				<executions>
					<execution>
						<id>src-jar</id>
						<phase>install</phase>
					</execution>
					<execution>
						<id>deploy</id>
						<phase>deploy</phase>
					</execution>
				</executions>
			</plugin>
			<plugin>
				<groupId>org.safris.xml.maven</groupId>
				<artifactId>binding</artifactId>
				<version>${xmlVersion}</version>
				<executions>
					<execution>
						<phase>generate-sources</phase>
						<goals>
							<goal>generate</goal>
						</goals>
					</execution>
				</executions>
				<configuration>
					<manifest xmlns="http://xml.safris.org/toolkit/binding/manifest.xsd">
						<link xmlns:xlink="http://www.w3.org/1999/xlink" xlink:href="${basedir}/src/main/resources/xml/manifest.xml"/>
					</manifest>
				</configuration>
			</plugin>
		</plugins>
	</build>
	<dependencies>
		<dependency>
			<groupId>org.safris.commons</groupId>
			<artifactId>util</artifactId>
			<version>${commonsVersion}</version>
		</dependency>
		<dependency>
			<groupId>org.safris.xml.generator</groupId>
			<artifactId>compiler</artifactId>
			<version>${xmlVersion}</version>
		</dependency>
	</dependencies>
</project>
